{% extends 'template.html.twig' %}

{% block body %}

    
    {{ form_start(form_wish)}}
   
    {{ form_widget(form_wish)}}

    <input class="btn btn-dark" type="submit" value="Valider"></input>

    {{ form_end(form_wish)}}
  

{% endblock %}
